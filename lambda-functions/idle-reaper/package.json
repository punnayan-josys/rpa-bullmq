{
  "name": "rpa-idle-reaper",
  "version": "1.0.0",
  "description": "Lambda function to terminate idle RPA sessions",
  "main": "index.js",
  "scripts": {
    "build": "npm install --production",
    "deploy": "zip -r idle-reaper.zip . && aws lambda update-function-code --function-name rpa-idle-reaper --zip-file fileb://idle-reaper.zip"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^18.0.0"
  },
  "keywords": [
    "rpa",
    "lambda",
    "redis",
    "session-management"
  ],
  "author": "RPA Team",
  "license": "MIT"
}
